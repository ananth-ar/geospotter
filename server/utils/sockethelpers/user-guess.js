// const { getdistanceandpoint } = require("../queries/mapqueries");
// const { markpoints } = require("../queries/userqueries");

// async function emitdistance(socket, name, mapname, urselection, index) {
//   const { point, distance } = await getdistanceandpoint(
//     mapname,
//     urselection,
//     index
//   );
//   await markpoints(name, point);
//   socket.emit("distance", distance);
// }

// module.exports = { emitdistance };
      